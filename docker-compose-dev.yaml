version: "3.8"

services:

  development:
    container_name: development
    build: .
    environment:
      - PUBLIC_KEYS=
        
    volumes:
      - "./data-local:/media/user"
    ports:
      - "22:22"
